spring:
  application:
    name: breathe
  datasource:
    url: jdbc:postgresql://localhost:5432/breathe_test
    username: breathe_admin
    password: password
  jpa:
    show-sql: true
  flyway:
    locations:
      - classpath:db/migration
